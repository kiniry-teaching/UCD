#ifndef EXAMPLE_H_
#define EXAMPLE_H_

/**
 * Brief description of class here.
 * Detailed description of class here
 */
class Example
{
public:
	Example();
	virtual ~Example();

	/**
	 * Insert brief description of function. Insert detailed description of
	 *  function. Can HTML or <code>...</code> tag as well as features
	 *  described in doxygen help
	 * Also fill the details of the following tags, note. the param and return
	 *  are 'required' even if purpose is obvious.
	 * @param       exParam1 Insert brief of exParam1
	 * @param       exParam2 Insert brief of exParam2
	 * @return      Insert brief of return value (methods only)
	 * @exception   Insert brief of exceptions thrown
	 * @author      Insert initials
	 * @version     ? 1.0 until after break, after we have a demo version?
	 * @see         pointer to references, produces "Also see: "
	 * @since       used if element is older than class/package
	 * @todo		Add todo description (will generate a todo list)
	 * @bug			Add bug description (will generate a bug list)
	 */
	int sampleMethod(int exParam1, int exParam2);
};

#endif /*EXAMPLE_H_*/
